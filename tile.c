#include <stdio.h>
#include <stdbool.h>

#include "tile.h"

int minesAroundTile(Tile* t) {
    return 0;
}

void printTile(Tile* t) {
    if (t->isHidden) {
        printf("▢ ");
    } else {
        
    }
}